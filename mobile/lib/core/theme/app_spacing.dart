/// Spacing tokens (Band 18: design system spacing scale). Use these
/// instead of raw numbers in `EdgeInsets`/`SizedBox`/`Gap`-style widgets so
/// the whole app's rhythm stays consistent and adjustable from one place.
class AppSpacing {
  const AppSpacing._();

  static const double xs = 4;
  static const double sm = 8;
  static const double md = 16;
  static const double lg = 24;
  static const double xl = 32;
  static const double xxl = 48;
}
